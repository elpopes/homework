#create a method that will take in two arrays that have the same elements except that
# one array has one extra element. Return that extra element.

# Hash maps will have linear runtime but take up a lot of memory
# An algorythm 

#use XOR on the arrays in their byte form and you'll get back 

#what's a word? 

#that doesn't make sense.