linked list is a uniary tree.
singly linked list.
all nodes have a value. @next. 
head is the first element and tail is the end.
class linked
    def initialize(val, )

    end
end

find. from the head
push 
unshifting 
doubly linked list @prev and @next
creates garbage when you delete.
[ ] with linkedlist is o(n)

doubly linked list
always have head and tail 
you change the head and tails prev and next sandwiching things inside.

Hash map has keys point to values.
From hash set to hash map. 
Each bucket is a link list.
 